# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build it-flashcards`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t it-flashcards`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system it-flashcards && \
          adduser --system -G it-flashcards it-flashcards

COPY dist/apps/it-flashcards it-flashcards
RUN chown -R it-flashcards:it-flashcards .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix it-flashcards --omit=dev -f install

CMD [ "node", "it-flashcards" ]
